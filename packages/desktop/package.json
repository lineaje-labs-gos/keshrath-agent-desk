{
  "name": "@agent-desk/desktop",
  "version": "1.7.0",
  "private": true,
  "description": "Electron shell for agent-desk. Hosts the main process + preload that bridge @agent-desk/core into Electron's IPC.",
  "type": "module",
  "main": "dist/main/index.js",
  "engines": {
    "node": ">=22.0.0 <23.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {
    "@agent-desk/core": "*"
  },
  "devDependencies": {
    "electron": "^35.0.0",
    "typescript": "^5.7.0"
  }
}
